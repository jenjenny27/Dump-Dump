<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <size
        android:width="200dp"
        android:height="30dp"
    />
    <corners android:radius="20dp" />
    <solid android:color="@color/switch_track_color" /> <!-- Track color -->
</shape>